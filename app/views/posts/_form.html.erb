<%= form_with model: @post, loacle: true do |f| %>
  <p><%= f.text_field :title %></p>
  <p><%= f.file_field :image, accept: "image/jpg image/png, image/jpeg, image/gif" %></p>
  <p><%= f.submit "送信" %></p>
<% end %>
